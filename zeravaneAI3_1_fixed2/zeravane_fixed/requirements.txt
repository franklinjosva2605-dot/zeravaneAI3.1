# ZeravaneAI v3.1 — Dependencies
# Install: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────────────────
streamlit>=1.35.0
python-dotenv>=1.0.0

# ── LLM — Gemini ──────────────────────────────────────────────────────────
google-genai>=0.3.0

# ── Backend API ───────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ── HTTP & Scraping ───────────────────────────────────────────────────────
requests>=2.32.0
beautifulsoup4>=4.12.0
urllib3>=2.0.0

# ── Vector Database ───────────────────────────────────────────────────────
# Pinned to 0.6.x — versions >=0.7 pull in opentelemetry-exporter-otlp-proto-grpc
# which breaks on Python 3.14 due to protobuf C extension incompatibility.
# The engine falls back to _InMemoryClient if chromadb fails to import anyway.
chromadb>=0.5.0,<0.7.0

# Pin protobuf to pure-Python-safe range (avoids C extension crash on Py3.14)
protobuf>=4.25.0,<5.0.0

# ── Data / Utils ──────────────────────────────────────────────────────────
pydantic>=2.0.0

# ── Crawl4AI (Tier-1 free scraper) ────────────────────────────────────────
# NOT listed here — install separately on local/Docker only:
#   pip install crawl4ai && crawl4ai-setup
#
# Reason: crawl4ai requires lxml which needs libxml2/libxslt system libs.
# Streamlit Cloud (Python 3.14) has no pre-built lxml wheel and no system
# libs, so the build fails. The engine handles absence gracefully — Tier 1
# is skipped and scraping falls through to Tier 2/3 automatically.
